import ShortUrlRedirect from '../components/common/ShortUrlRedirect';

const DealershipShortURL = () => {
  return <ShortUrlRedirect targetPath="/bayilik" targetName="Bayilik" delay={3000} />;
};

export default DealershipShortURL;
