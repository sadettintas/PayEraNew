import ShortUrlRedirect from '../components/common/ShortUrlRedirect';

const ContactShortURL = () => {
  return <ShortUrlRedirect targetPath="/iletisim" targetName="İletişim" delay={3000} />;
};

export default ContactShortURL;
